# Rust
/target/
**/*.rs.bk
.tmp/

# Runtime (camera refs, outbox, logs — local only)
/data/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
*.iml

# Keep .env in this private repo so Pi/laptop deploy has keys.
# Rotate the key if the repo is ever made public.
*.pem
